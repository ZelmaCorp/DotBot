# Docker Environment Configuration
# Use this for Docker Compose deployments

# =============================================================================
# CORE CONFIGURATION
# =============================================================================

NODE_ENV=production
DEBUG=false
LOG_LEVEL=info

# =============================================================================
# APPLICATION URLs
# =============================================================================
# NOTE: Base URLs should NOT include /api suffix
# These are used as build args for frontend Docker image

REACT_APP_API_URL=https://example.com
REACT_APP_WS_URL=wss://example.com

# Runtime URLs (for backend)
BACKEND_URL=https://example.com
FRONTEND_URL=https://example.com
WS_URL=wss://example.com

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================

AI_PROVIDER=asi-one
ASI_ONE_API_KEY=your_asi_one_api_key_here
ASI_ONE_ENDPOINT=https://agentverse.ai/api/v1
ASI_ONE_BASE_URL=https://api.asi1.ai/v1

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

CORS_ORIGINS=https://example.com

# =============================================================================
# STORAGE
# =============================================================================

STORAGE_DIR=/app/backend/data/storage
